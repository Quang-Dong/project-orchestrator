interface:
  display_name: "Project Orchestrator"
  short_description: "Coordinate authorized subagents for product outcomes"
  default_prompt: "Use $project-orchestrator to coordinate substantive work through authorized subagents, honor existing execution choices, and verify the integrated outcome."
policy:
  allow_implicit_invocation: true
