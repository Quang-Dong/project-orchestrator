interface:
  display_name: "Project Orchestrator"
  short_description: "Guide agent execution to verified product outcomes"
  default_prompt: "Use $project-orchestrator to resolve the next consequential product decision, act within authority, and verify the actual outcome."
policy:
  allow_implicit_invocation: true
